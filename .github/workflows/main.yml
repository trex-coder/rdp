name: CI
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest

    steps:

    - name: Enable RDP
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
        Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force)

    - name: Download cloudflared
      run: |
        Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe

    - name: Start Cloudflare Tunnel
      shell: powershell
      run: |
        Write-Host "Starting Cloudflare Tunnel..."

        # Start cloudflared and capture stdout to a file
        $process = Start-Process -FilePath ".\cloudflared.exe" `
            -ArgumentList "tunnel --url tcp://localhost:3389" `
            -RedirectStandardOutput output.txt `
            -NoNewWindow `
            -PassThru

        # Wait for URL to appear
        Write-Host "Waiting for tunnel URL..."
        $url = $null
        for ($i=0; $i -lt 30; $i++) {
            Start-Sleep -Seconds 1
            if (Test-Path output.txt) {
                $match = (Get-Content output.txt | Select-String "tcp://")
                if ($match) {
                    $url = $match.Matches[0].Value
                    break
                }
            }
        }

        if (-Not $url) {
            Write-Host "Failed to obtain tunnel URL."
            Get-Content output.txt
            exit 1
        }

        Write-Host "Tunnel URL: $url"

        # Keep process alive and print tunnel repeatedly
        while ($true) {
            Start-Sleep -Seconds 30
            Write-Host "Tunnel still active: $url"
        }
