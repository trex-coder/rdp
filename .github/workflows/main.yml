name: CI
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest

    steps:

    - name: Enable RDP
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
        Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "P@ssw0rd!" -Force)

    - name: Download cloudflared
      run: |
        Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe

    - name: Start Cloudflare Tunnel
      shell: powershell
      run: |
        Write-Host "Starting Cloudflare Tunnel..."

        # Cleanup old logs
        if (Test-Path "stdout.txt") { Remove-Item "stdout.txt" }
        if (Test-Path "stderr.txt") { Remove-Item "stderr.txt" }

        # Start cloudflared with separate stdout/stderr logs
        $proc = Start-Process -FilePath ".\cloudflared.exe" `
            -ArgumentList "tunnel --url tcp://localhost:3389" `
            -NoNewWindow `
            -RedirectStandardOutput "stdout.txt" `
            -RedirectStandardError "stderr.txt" `
            -PassThru

        Write-Host "Waiting for tunnel hostname..."

        $hostname = $null

        while ($true) {
            Start-Sleep -Seconds 2

            # Check both stdout and stderr logs
            $lines = @()
            if (Test-Path "stdout.txt") { $lines += Get-Content stdout.txt }
            if (Test-Path "stderr.txt") { $lines += Get-Content stderr.txt }

            # Cloudflare always prints https://xxxxx.trycloudflare.com
            foreach ($line in $lines) {
                if ($line -match "https://.*trycloudflare.com") {
                    $hostname = $line.Trim().Replace("https://", "")
                    break
                }
            }

            if ($hostname) { break }
        }

        if (-not $hostname) {
            Write-Host "Could not detect tunnel hostname"
            exit 1
        }

        $tcpurl = "tcp://$hostname:3389"

        Write-Host "==========================================="
        Write-Host " Cloudflare Tunnel Ready"
        Write-Host " RDP URL: $tcpurl"
        Write-Host "==========================================="

        # Keep job alive
        while ($true) {
            Start-Sleep -Seconds 30
            Write-Host "Tunnel still active: $tcpurl"
        }
